var createCounter = function (n) {
    current = n
    return function () {

        return (n, current++)
        

    };
};


const counter = createCounter(10)
counter() // 10
counter() // 11
counter() // 12
